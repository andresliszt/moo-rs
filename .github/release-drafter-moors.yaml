name-template: "moors $RESOLVED_VERSION"
tag-template:  "moors-$RESOLVED_VERSION"
tag-prefix:    "moors-"

# Only include PRs that touched files in this directory
include-paths:
  - "moors/"

# Exclude PRs carrying these labels
exclude-labels:
  - relnotes-ignore

# Group PRs by labels (section headings)
categories:
  - title: New Features
    labels: [relnotes-feature]
  - title: Changes
    labels: [relnotes-change]
  - title: Performance
    labels: [relnotes-performance]
  - title: Fixes
    labels: [relnotes-fix]
  # Fallback so uncategorized PRs still appear
  - title: Other Changes
    labels: ["*"]

# Sort entries newest-first
sort-by: merged_at
sort-direction: descending

version-resolver:
  major:   { labels: ["major"] }
  minor:   { labels: ["minor"] }
  patch:   { labels: ["patch"] }
  default: patch

# Only consider previous releases pointing to the same target
filter-by-commitish: true

# Per-change line format
change-title-escapes: '\<*_&'
change-template: "- $TITLE (#$NUMBER) @$AUTHOR"

template: |
  ## What’s Changed
  $CHANGES

  ## Contributors
  $CONTRIBUTORS

no-changes-template: "* No changes"
